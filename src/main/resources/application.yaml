mybatis:
  mapper-locations: classpath:mybatis/mapper/*.xml
  type-aliases-package: com.mulun.pojo

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/mulun_backstage?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver